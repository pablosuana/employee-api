async-cache-db {
  database {
    host = "jdbc:h2:mem:employee_table"
    database = "employee_table;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_LOWER=TRUE;INIT=RUNSCRIPT FROM './src/main/resources/schema.sql'\\;RUNSCRIPT FROM './src/main/resources/data.sql'"
    port = 0
    username = "sa"
    password = ""
    table-name = "employee_table"

    }
}

async-http-client {
  dispatcher {
    type = Dispatcher
    executor = fork-join-executor
    throughput = 1
  }
}